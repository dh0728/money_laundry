# 선정 후보 35개 피처 명세


- 노드: 은행+계좌로 정의. A는 송신 노드, B는 수신 노드, t는 현재 거래 시각.
- L(x)=ln(1+x), G(Δ)=ln(2+Δ). Δ 단위는 분이며 과거 거래가 없으면 간격 피처는 0.
- 노드 행동 통계는 당일을 포함한 최근 30개 달력일 [D−29일,D] 전체 거래로 계산. 초기에는 관측 가능한 기간만 사용합니다. 당일 마감 모델이며 실시간 시점별 피처가 아닙니다. KYC 유형은 계좌 등록정보에서 추출합니다.
- 거래 리듬 피처는 현재 시각보다 최소 1분 이전 이력만 사용. 1시간 창은 [t−60분,t−1분]. 전체 누적 피처는 30일로 제한하지 않습니다.
- paid_currency_z: 통화별 보정 기간의 송금·수취 금액을 합쳐 기준을 적합합니다. s=양수 금액 중앙값, u=ln(1+a/s), m=median(u), q=max(IQR(u),0.001), z=clip((u−m)/q,−8,8). 보정 기간은 2022-08-01~08-07이며 기준을 고정합니다. 미관측 통화는 z=0. 표준편차 기반 z-score가 아닙니다.
- 모든 입력 dtype은 float32. 이진 피처도 0.0/1.0으로 입력합니다. 표 순서는 각 입력 텐서의 열 순서입니다. 노드 19개는 송신·수신·이웃 계좌에 같은 스키마를 적용하고 엣지 16개는 문맥 거래와 평가 대상 거래에 같은 스키마를 적용합니다.

## 노드 피처 19개

| 순서 | 피처 | 계산 정의 |
|---|---|---|
| 1 | `log_out_count` | L(30일 문맥의 송금 거래 수). 자기 이체도 포함. |
| 2 | `log_in_count` | L(30일 문맥의 수취 거래 수). 자기 이체도 포함. |
| 3 | `log_out_neighbors` | L(서로 다른 수신 노드 수). 자기 이체가 있으면 자신도 상대 집합에 포함. |
| 4 | `log_in_neighbors` | L(서로 다른 송신 노드 수). 자기 이체가 있으면 자신도 상대 집합에 포함. |
| 5 | `log_self_count` | L(src=dst인 송금 거래 수). 다계좌 순환 횟수가 아님. |
| 6 | `log_reciprocal_neighbors` | L(A→B와 B→A가 모두 존재하는 상대 B 수). A 자신은 제외. |
| 7 | `mean_paid_currency_z` | 30일 송금 거래의 paid_currency_z 평균. 송금이 없으면 0. |
| 8 | `paid_outlier_fraction` | 30일 송금 중 abs(paid_currency_z) ≥ 3인 비율. 송금이 없으면 0. |
| 9 | `log_currency_count` | L(송금 통화와 수취 통화의 합집합 크기). 거래 수가 아니라 통화 종류 수. |
| 10 | `log_active_days` | L(송금 또는 수취가 한 번 이상 발생한 날짜 수). 연속일 수가 아님. |
| 11 | `type_Corporation` | 계좌 등록정보 entity_name에서 추출한 유형이 Corporation이면 1, 아니면 0. 이름 패턴 기반 원핫 인코딩. |
| 12 | `type_Partnership` | 계좌 등록정보 entity_name에서 추출한 유형이 Partnership이면 1, 아니면 0. 이름 패턴 기반 원핫 인코딩. |
| 13 | `type_Sole Proprietorship` | 계좌 등록정보 entity_name에서 추출한 유형이 Sole Proprietorship이면 1, 아니면 0. 이름 패턴 기반 원핫 인코딩. |
| 14 | `type_Individual` | 계좌 등록정보 entity_name에서 추출한 유형이 Individual이면 1, 아니면 0. 이름 패턴 기반 원핫 인코딩. |
| 15 | `type_Country` | 계좌 등록정보 entity_name에서 추출한 유형이 Country이면 1, 아니면 0. 이름 패턴 기반 원핫 인코딩. |
| 16 | `type_Direct` | 계좌 등록정보 entity_name에서 추출한 유형이 Direct이면 1, 아니면 0. 이름 패턴 기반 원핫 인코딩. |
| 17 | `log_entity_observed_accounts` | L(같은 entity_id에 속하며 30일 문맥에서 활동한 계좌 수). 전체 등록 계좌 수가 아님. ID 미상은 0. |
| 18 | `log_entity_out_diversity` | L(해당 엔티티가 송금한 서로 다른 상대 엔티티 수). 양쪽 ID가 알려진 거래만 집계하며 같은 엔티티도 포함 가능. |
| 19 | `log_entity_in_diversity` | L(해당 엔티티가 수취한 서로 다른 상대 엔티티 수). 양쪽 ID가 알려진 거래만 집계하며 같은 엔티티도 포함 가능. |

## 거래(엣지) 피처 16개

| 순서 | 피처 | 계산 정의 |
|---|---|---|
| 1 | `pair_prior_count` | L(현재 t보다 최소 1분 앞선 A→B의 전체 누적 거래 수). 30일로 제한하지 않음. |
| 2 | `pair_gap_minutes` | A→B의 마지막 이전 거래와 t의 간격 Δ분을 G(Δ)로 변환. 같은 분 제외, 이전 거래가 없으면 0. |
| 3 | `pair_count_1h` | L([t−60분, t−1분] 구간의 A→B 거래 수). 양 끝 포함. |
| 4 | `sender_gap_minutes` | A의 마지막 이전 송금과 t의 간격을 G(Δ)로 변환. 상대 계좌 무관, 이력 없으면 0. |
| 5 | `sender_count_1h` | L([t−60분, t−1분] 구간에서 A가 보낸 거래 수). 상대 계좌 무관. |
| 6 | `receiver_gap_minutes` | B의 마지막 이전 수취와 t의 간격을 G(Δ)로 변환. 송신 계좌 무관, 이력 없으면 0. |
| 7 | `receiver_count_1h` | L([t−60분, t−1분] 구간에서 B가 받은 거래 수). 송신 계좌 무관. |
| 8 | `paid_currency_z` | 현재 송금액의 통화별 강건 상대 점수 z. 중앙값·IQR 기준이며 표준편차 z-score가 아님. |
| 9 | `pair_history_count` | L(당일 00:00 이전 A→B 누적 거래 수). 당일 발생분은 제외하며 30일 제한 없음. |
| 10 | `reverse_history_count` | L(당일 00:00 이전 B→A 누적 거래 수). 자기 이체에서는 정방향과 같은 관계. |
| 11 | `pair_age_at_day_start` | 당일 00:00과 마지막 이전 A→B 거래 사이 경과 분에 G(Δ) 적용. 이전 거래 없으면 0. 최초 거래 이후 나이가 아님. |
| 12 | `same_entity` | 송신 entity_id가 알려져 있고 수신 entity_id와 같으면 1, 아니면 0. |
| 13 | `same_known_country` | 은행명에서 추출한 두 국가가 모두 알려져 있고 같으면 1. 국가 미상도 0이므로 단독으로 국경 간 거래를 뜻하지 않음. |
| 14 | `both_countries_known` | 송신·수신 은행명의 국가 추출값이 모두 UNKNOWN이 아니면 1. |
| 15 | `same_currency` | 현재 거래의 payment_currency=receiving_currency이면 1, 아니면 0. |
| 16 | `is_self_transfer` | 동일 은행·계좌로 정의한 송신 노드와 수신 노드가 같으면 1. |

## 제외한 기존 피처 5개

`paid_currency_unseen`, `received_currency_unseen`, `mean_received_currency_z`, `received_outlier_fraction`, `reciprocity_ratio`


## IG 설명과 선정 근거 표

**IG(Integrated Gradients)는 기준 입력에서 실제 입력으로 이동할 때 모델 출력이 얼마나 달라지는지, 그 차이를 각 입력 피처의 기여로 나누어 설명하는 방법입니다.** 이 경로에서 기울기를 여러 지점에 걸쳐 계산해 누적합니다. 모델을 다시 학습하지 않고 이미 학습된 모델의 판단을 설명합니다. [Captum 공식 설명](https://captum.ai/docs/extension/integrated_gradients)

| 궁금한 점 | 쉽게 읽는 방법 |
|---|---|
| 무엇을 설명하나요? | 이번 분석은 거래의 자금세탁 점수 중 확률 변환 전 값인 **logit**을 설명합니다. IG 값은 확률이나 정확도 퍼센트가 아닙니다. |
| 무엇과 비교하나요? | 거래마다 정상 거래 참조 16개를 사용했습니다. 대상 거래·송수신 노드는 참조 거래의 값으로, 이웃 노드·문맥 거래는 해당 날짜의 표본 값으로 기준을 구성했습니다. 연결 구조는 고정하므로 완전한 실제 정상 그래프와 비교하는 것은 아닙니다. |
| IG가 양수이면? | 그 입력·참조 조합에서 해당 피처의 기여가 설명 대상 logit을 올리는 방향입니다. 모든 거래에서 그 피처의 값이 클수록 위험하다는 뜻은 아닙니다. |
| IG가 음수이면? | 기준 입력에 비해 logit을 낮추는 방향의 기여입니다. 실제 부정확한 예측을 낮출 때 유용할 수 있지만, 음수라는 사실만으로 정상 탐지 효과가 검증되는 것은 아닙니다. |
| 표의 순위는 무엇인가요? | 16개 참조의 기여를 평균하고, 같은 피처의 노드/엣지 경로 기여를 **부호를 유지해 합산한 뒤 절댓값**을 취해 거래 집단 내 평균을 냈습니다. 이 값이 큰 순서입니다. 경로·참조 사이 상쇄가 있을 수 있습니다. |
| 순위가 낮으면 삭제해도 되나요? | 아닙니다. 특정 집단이나 날짜에서 유용할 수 있어 순열 교란과 여러 피처의 동시 상수화 검사를 함께 봤습니다. |
| 왜 IG와 AP 손실을 같이 보나요? | IG는 **현재 예측의 설명 기여**, 순열 교란은 **정보를 흐트러뜨렸을 때 평가 성능 변화**입니다. 서로 다른 질문을 측정하므로 순위가 같을 필요가 없습니다. |

계산식: `IG_i(x; b) = (x_i − b_i) × ∫₀¹ ∂F(b + α(x − b))/∂x_i dα`.
`x`는 실제 입력, `b`는 기준 입력, `F`는 모델 출력입니다. 충분히 정확하게 적분하면 기여의 합은 `F(x) − F(b)`와 일치하며, 수치 근사 잔차로 계산 정확도를 확인합니다. [Captum 완전성·수렴 설명](https://captum.ai/docs/extension/integrated_gradients)

**이번 분석의 표본과 한계**

| 항목 | 실제 설정·해석 |
|---|---|
| IG 표본 | 검증 5일, 1,000건: 패턴 연결 양성 300·미연결 양성 300·정상 300·오탐 100. 아래 표에는 앞의 세 집단을 표시합니다. 모집단 비율을 재현한 표본이 아니므로 집단 수를 합쳐 전체 중요도로 해석하지 않습니다. |
| 연결 / 미연결 | 패턴 메타데이터에 매칭되는 양성 / 매칭되지 않는 양성. 그래프 연결·고립 여부가 아닙니다. |
| 순위의 분모 | 원래 40개 입력 중 유형 원핫 6열을 한 그룹으로 묶은 **35개 설명 단위**. 최종 선정한 35개 입력과 숫자만 같으며 같은 목록은 아닙니다. 작은 순위가 높은 기여입니다. |
| 계좌 유형 6열 | **묶음 순위를 각 행에 ‘공통’으로 표시**했습니다. 여섯 개의 개별 순위가 아니며 IG 값을 여섯 번 더하면 안 됩니다. |
| 수렴 검사 | 16,000개 거래·참조 쌍 중 15,485개 통과, 515개 미달. 모든 참조가 통과한 거래는 679/1,000건. 표는 원 분석의 전체 표본 집계이며 완전 수렴 결과는 아닙니다. 통과 거래만 집계한 그룹별 순위 상관은 0.970~0.999였습니다. |
| AP 감소 | 별도 검증 50,000건의 IPW 평가에서 피처별 순열 교란 3회 평균. 단위 %p. 양수는 성능 악화이며 피처별 손실을 더하지 않습니다. |
| 추가 제외 후보의 손실 | 최종 35개에서 해당 피처 또는 그룹을 더 상수화한 후보를 **원본 40개와 비교**한 최악 날짜·기준값의 손실. 35개 대비 추가 손실이 아닙니다. |
| 최종 선정 | 저기여 8그룹의 256조합 중 날짜별·합산·두 상수 기준을 통과한 최소 입력 수 35개. AP 손실 ≤0.5%p, 정밀도·재현율·상위 1% 재현율·미연결 양성 상위 1% 재현율 손실 각각 ≤1%p라는 임시 기준을 사용했습니다. 전역 최적성·개별 필수성은 미확정입니다. |
| 패턴 열 | 계산 정의에서 추론한 거래 신호입니다. 패턴별 탐지율, 자금세탁 인과관계, 재학습 후 효과를 직접 검증한 결과가 아닙니다. |

### 노드 피처 19개

| 피처 | IG 순위: 연결 / 미연결 / 정상 | 교란 시 AP 감소(%p) | 유지 이유 | 관련 거래 신호 |
|---|---|---:|---|---|
| `log_out_count` | 13 / 7 / 12 | 1.112 | 미연결 양성 IG 7위; 송금 활동 규모를 설명 | 다량 송금 계좌, 반복 송금 활동 |
| `log_in_count` | 16 / 13 / 8 | 1.868 | 정상 IG 8위; 수취 활동의 정상·양성 맥락 구분에 활용 | 다량 입금 계좌, 수취 집중 |
| `log_out_neighbors` | 18 / 14 / 17 | 1.033 | 교란 시 미연결 양성 상위 1% 재현율 −2.35%p | 한 계좌에서 여러 계좌로 보내는 분산 송금(fan-out) |
| `log_in_neighbors` | 20 / 11 / 13 | 0.230 | 미연결 양성 IG 11위; 수취 상대 구조를 설명 | 여러 계좌에서 한 계좌로 모이는 집금(fan-in) |
| `log_self_count` | 28 / 28 / 25 | 0.308 | IG는 낮음; 추가 제외 후보의 최악 AP 손실 0.976%p로 기준 초과 | 자기 계좌 이체가 반복되는 활동 |
| `log_reciprocal_neighbors` | 10 / 22 / 26 | 3.605 | 연결 양성 IG 10위; 교란 시 AP −3.60%p | A↔B 형태로 주고받는 상대 관계 |
| `mean_paid_currency_z` | 22 / 18 / 18 | 0.757 | 교란 시 AP −0.76%p, 미연결 양성 상위 1% 재현율 −1.05%p | 해당 통화 기준으로 평균적으로 큰 금액을 보내는 계좌 |
| `paid_outlier_fraction` | 31 / 29 / 31 | 0.012 | IG는 낮음; 추가 제외 후보의 최악 정밀도 손실 2.900%p | 통화 기준에서 매우 크거나 작은 금액의 반복 송금 |
| `log_currency_count` | 24 / 33 / 32 | 0.756 | IG는 낮음; 교란 시 AP −0.76%p로 보완 근거 | 여러 통화를 사용하는 계좌 활동 |
| `log_active_days` | 26 / 23 / 27 | 0.042 | 추가 제외 후보의 미연결 양성 상위 1% 재현율 손실 최대 5.776%p | 짧은 기간에 활동이 집중된 계좌와 꾸준히 활동하는 계좌 구분 |
| `type_Corporation` | 23 / 20 / 22 (6열 공통) | 0.083† | 유형 6열 묶음 유지: 추가 제외 후보의 미연결 양성 상위 1% 재현율 손실 최대 2.046%p | 등록 이름에서 추출한 Corporation 유형의 거래 맥락 |
| `type_Partnership` | 23 / 20 / 22 (6열 공통) | 0.088† | 유형 6열 묶음 유지: 추가 제외 후보의 미연결 양성 상위 1% 재현율 손실 최대 2.046%p | 등록 이름에서 추출한 Partnership 유형의 거래 맥락 |
| `type_Sole Proprietorship` | 23 / 20 / 22 (6열 공통) | 0.036† | 유형 6열 묶음 유지: 추가 제외 후보의 미연결 양성 상위 1% 재현율 손실 최대 2.046%p | 등록 이름에서 추출한 Sole Proprietorship 유형의 거래 맥락 |
| `type_Individual` | 23 / 20 / 22 (6열 공통) | 0.012† | 유형 6열 묶음 유지: 추가 제외 후보의 미연결 양성 상위 1% 재현율 손실 최대 2.046%p | 등록 이름에서 추출한 Individual 유형의 거래 맥락 |
| `type_Country` | 23 / 20 / 22 (6열 공통) | 0.008† | 유형 6열 묶음 유지: 추가 제외 후보의 미연결 양성 상위 1% 재현율 손실 최대 2.046%p | 등록 이름에서 추출한 Country 유형의 거래 맥락 |
| `type_Direct` | 23 / 20 / 22 (6열 공통) | 0.002† | 유형 6열 묶음 유지: 추가 제외 후보의 미연결 양성 상위 1% 재현율 손실 최대 2.046%p | 등록 이름에서 추출한 Direct 유형의 거래 맥락 |
| `log_entity_observed_accounts` | 11 / 8 / 14 | 1.872 | 미연결 양성 IG 8위; 해당 집단 상위 1% 재현율 교란 손실 3.55%p | 같은 소유 주체가 여러 계좌를 사용하는 활동 |
| `log_entity_out_diversity` | 15 / 9 / 15 | 0.542 | 미연결 양성 IG 9위; 해당 집단 상위 1% 재현율 교란 손실 3.63%p | 한 소유 주체가 여러 다른 주체로 송금하는 분산 구조 |
| `log_entity_in_diversity` | 8 / 4 / 10 | 1.074 | 미연결 양성 IG 4위; 해당 집단 상위 1% 재현율 교란 손실 7.83%p | 여러 주체의 자금이 한 소유 주체로 모이는 집금 구조 |

### 거래 피처 16개

| 피처 | IG 순위: 연결 / 미연결 / 정상 | 교란 시 AP 감소(%p) | 유지 이유 | 관련 거래 신호 |
|---|---|---:|---|---|
| `pair_prior_count` | 2 / 2 / 2 | 31.903 | 양성 두 집단 모두 IG 2위; 반복 이력의 핵심 근거 | 같은 방향 A→B 거래의 반복·익숙한 관계 |
| `pair_gap_minutes` | 3 / 17 / 19 | 29.992 | 연결 양성 IG 3위; 교란 시 AP −29.99%p | A→B의 짧은 간격 재거래 또는 긴 공백 후 재거래 |
| `pair_count_1h` | 6 / 6 / 4 | 2.430 | 양성 두 집단 모두 IG 6위; 단기 반복의 설명 기여 | 한 시간 안에 반복되는 A→B 송금 |
| `sender_gap_minutes` | 12 / 10 / 7 | 1.410 | 미연결 양성 IG 10위, 정상 IG 7위 | 상대가 바뀌어도 연속되는 빠른 송금 또는 공백 후 송금 |
| `sender_count_1h` | 14 / 3 / 3 | 1.350 | 미연결 양성·정상 IG 모두 3위; 집단별 핵심 근거 | 한 시간 안에 여러 번 보내는 송금 급증 |
| `receiver_gap_minutes` | 17 / 21 / 16 | 2.240 | IG 중위권; 교란 시 AP −2.24%p로 유지 보강 | 여러 송신자의 거래를 포함한 빠른 연속 입금 또는 공백 후 입금 |
| `receiver_count_1h` | 27 / 12 / 11 | 5.249 | 연결 양성 IG는 낮지만 정상 11위; 교란 시 AP −5.25%p | 한 시간 안에 몰리는 다량 입금 |
| `paid_currency_z` | 7 / 16 / 9 | 8.435 | 연결 양성 IG 7위; 교란 시 AP −8.44%p | 통화 기준에 비해 이례적으로 크거나 작은 송금 |
| `pair_history_count` | 1 / 1 / 1 | 33.584 | 세 집단 모두 IG 1위; 가장 높은 설명 기여 | 기존 거래 관계와 신규 관계 구분, 장기간 반복 관계 |
| `reverse_history_count` | 4 / 5 / 5 | 25.438 | 연결 양성 IG 4위, 미연결·정상 5위 | B→A 이력이 있는 상태에서 발생한 A→B 거래 |
| `pair_age_at_day_start` | 5 / 19 / 20 | 36.945 | 연결 양성 IG 5위; 교란 시 AP −36.94%p | 최근까지 거래한 상대와 오랜 공백이 있는 상대 구분 |
| `same_entity` | 21 / 24 / 23 | 2.667 | IG는 낮지만 교란 시 AP −2.67%p | 같은 소유 주체의 계좌 사이 이체와 외부 주체 송금 구분 |
| `same_known_country` | 25 / 30 / 28 | 2.282 | IG는 낮지만 교란 시 AP −2.28%p | 같은 국가 은행 사이 거래와 다른 국가 은행 사이 거래의 구분 보조 |
| `both_countries_known` | 29 / 27 / 29 | 0.087 | IG는 낮음; 추가 제외 후보 정밀도 손실 1.005%p로 1%p 기준 근소 초과 | 국가가 달라 same_known_country=0인 경우와 국가 미상으로 0인 경우 구별 |
| `same_currency` | 19 / 25 / 21 | 1.866 | 연결 양성 IG 19위; 교란 시 AP −1.87%p | 송금 통화와 수취 통화가 다른 거래의 구분 |
| `is_self_transfer` | 9 / 15 / 6 | 10.235 | 정상 IG 6위; 교란 시 AP −10.23%p로 유지 근거 | 동일 은행·동일 계좌로 보내는 거래 구분 |

† 유형 피처의 AP는 **단일 열 순열 교란** 수치이고 IG·추가 제외 근거는 **6열 묶음**입니다. 단일 원핫 열 교란은 유효하지 않은 유형 조합을 만들 수 있으므로 보조 근거로만 읽습니다.

### IG 값 자체를 보고 싶다면

아래 값은 위 순위에 사용한 **평균 절대 순기여(logit 단위)**입니다. 값의 부호 방향은 이 표에서 알 수 없습니다. 집단별 표본과 기준 입력이 다르므로 집단 사이의 값 차이를 보편적인 중요도 차이로 해석하지 않습니다.

| 설명 단위 | 연결 양성 | 미연결 양성 | 정상 |
|---|---:|---:|---:|
| `log_out_count` | 0.3977 | 0.6552 | 0.5215 |
| `log_in_count` | 0.2926 | 0.4240 | 0.6884 |
| `log_out_neighbors` | 0.2613 | 0.3800 | 0.3673 |
| `log_in_neighbors` | 0.2107 | 0.4909 | 0.4841 |
| `log_self_count` | 0.1029 | 0.0654 | 0.1517 |
| `log_reciprocal_neighbors` | 0.5307 | 0.1787 | 0.1434 |
| `mean_paid_currency_z` | 0.2067 | 0.2373 | 0.3596 |
| `paid_outlier_fraction` | 0.0433 | 0.0630 | 0.0623 |
| `log_currency_count` | 0.1921 | 0.0213 | 0.0380 |
| `log_active_days` | 0.1683 | 0.1718 | 0.1364 |
| `type_*(6열)` | 0.2003 | 0.1962 | 0.2582 |
| `log_entity_observed_accounts` | 0.4449 | 0.5469 | 0.4760 |
| `log_entity_out_diversity` | 0.3612 | 0.5366 | 0.4439 |
| `log_entity_in_diversity` | 0.5603 | 0.9361 | 0.5896 |
| `pair_prior_count` | 5.8483 | 3.0433 | 1.7559 |
| `pair_gap_minutes` | 1.5492 | 0.3044 | 0.3295 |
| `pair_count_1h` | 0.9593 | 0.7237 | 1.1212 |
| `sender_gap_minutes` | 0.4340 | 0.5164 | 0.7754 |
| `sender_count_1h` | 0.3930 | 1.1043 | 1.2493 |
| `receiver_gap_minutes` | 0.2701 | 0.1843 | 0.3794 |
| `receiver_count_1h` | 0.1636 | 0.4447 | 0.5374 |
| `paid_currency_z` | 0.6295 | 0.3086 | 0.6049 |
| `pair_history_count` | 6.0672 | 3.2111 | 1.7568 |
| `reverse_history_count` | 1.1314 | 0.7513 | 0.8811 |
| `pair_age_at_day_start` | 1.0803 | 0.2206 | 0.2904 |
| `same_entity` | 0.2087 | 0.1068 | 0.2442 |
| `same_known_country` | 0.1742 | 0.0531 | 0.0992 |
| `both_countries_known` | 0.0754 | 0.0681 | 0.0804 |
| `same_currency` | 0.2398 | 0.0777 | 0.2896 |
| `is_self_transfer` | 0.5463 | 0.3302 | 0.8144 |

### 해석상의 주의와 원자료

IG가 높은 피처는 현재 모델의 설명에서 비중이 크다는 뜻.
