---
날짜: 2026-09-16
이름: 손은총
주요 담당 업무: 자금 세탁 탐지 AI 모델 개발 (2차 — 9-Class 다중분류)
---

## 실적

- 완료한 일 + 산출물 경로/이슈 번호 :

어젯밤 M5 묶음 빼보기 36팔이 다 끝나서(09-15 17:53 UTC, 실패 0) 오늘은 그 결과를 41열 판정표에 합치는 것부터 시작했다. 대조군 41열 정밀도는 세 시드 평균 0.8455이고, 시드끼리 흔들리는 폭은 최대 0.14%p(표준편차 0.07%p)다. 이 폭보다 크게 떨어지면 "필수", 안 떨어지면 "빼도 됨"으로 본다.

- 묶음별 정밀도 변화: 계좌쌍 4열 −73.6%p, 금액 2열 −17.6, 누적 3열 −15.8, 수신 이력 2열 −8.4, 송신 이력 2열 −4.0, 자기송금 −2.8, 통화 15열 −1.6, 교차 2열 −1.1, 이력 z 3열 −0.2. 반대로 송신 속도 4열 +0.05, 구조화·반올림 3열 +0.11, 수신 속도 3열 +0.21은 빼도 차이가 없거나 오히려 올랐다.
- 판정표 생성 코드(`m6_verdict.py`)에서 "오름"을 "떨어짐"으로 표시하던 분기 버그를 찾아 고치고 표를 다시 조립했다. 검토 노트북도 다시 만들어 그림 5장(정답 대조 우도비, 시계 gain·10분위 추이, 필요성 점그림, 정당성×필요성 지도 2장)을 넣었다.

이어서 묶음 안의 열을 하나씩 갈라서 실측했다(전부 시드 42, 새 채점 방식 `ooc_arm_fast.py` 재사용).

- 송신 누적 차수만 제거 −3.81%p, 수신 누적 차수만 제거 −7.86%p, 둘 다 −10.28%p. 송신 이력 묶음 손해(−3.96%p)의 96%를 송신 누적 차수 한 열이 진다. gain 비중이 거의 같은 두 열(2.45% vs 2.43%)이라 손해도 반씩일 거라 짐작했는데 틀렸다. M4 재구성 정확도(98.4% vs 47.1%)가 gain보다 훨씬 나은 예측 근거였다.
- 송신 경과시간만 제거 +0.59%p(오히려 이득), 수신 경과시간만 제거 −0.59%p. 송신·수신 비대칭이 경과시간 열까지 이어진다.
- 43열에서 41열로 갈 때 함께 뺀 두 열을 갈라 쟀다. 43열 기준 정밀도 0.8580에서 통화 불일치만 빼면 −0.45%p, 야간·현금 플래그만 빼면 −0.81%p. 09-14에 둘을 한꺼번에 뺀 비용 −1.26%p가 정확히 둘의 합이었다.
- M4 확장: 열 하나를 재구성할 때 몇 개 열이 동원되는지 셌다. 송신 1시간·24시간 건수는 2열로 gain의 90%가 채워지는 반면 수신 누적 차수는 10열, 자기송금은 14열이 필요했다. 기존 M4 재현 검증은 27개 중 25개가 일치했고 반올림 플래그·수신 누적 차수 두 열은 최대기여열이 달라져 불안정한 것으로 남겼다.
- 81열에서 41열까지 온 여정을 한 장 그림으로 만들었다(단계마다 뺀 열·근거·정밀도).

오후 회의에서 "거래 건수 창을 1시간과 24시간으로 정한 이유가 뭐냐"는 질문을 받았는데 답하지 못했다. 코드를 확인하니 `W_1H=60`, `W_24H=1440`이 상수로만 박혀 있고 근거 기록이 어디에도 없었다. 그래서 세탁이 실제로 어느 시간 규모에서 일어나는지 패턴 원문에서 직접 재봤다(M9).

- 세탁 시도 16,467개(거래 137,936건)를 파싱했다. 거래 2건 이상인 시도의 지속 시간 중앙값은 29.6일이다. 1시간 안에 끝나는 시도는 0.28%, 24시간 안은 3.49%, 7일 안은 8.43%, 30일 안은 51.4%다.
- 한 시도 안에서 연속 거래 사이 간격의 중앙값은 2.7일이다. 24시간 창은 같은 시도의 직전 거래를 13%만 보고, 7일 창은 77%, 30일 창은 98.5%를 본다. 시도 안 최소 간격이 3분 이하인 경우는 0.51%다.
- 결론: 1시간·24시간 창은 이 데이터의 세탁 시간 규모와 맞지 않는다. 회의 질문에 대한 답은 "처음에 관행적으로 잡은 값이었고, 실측하니 세탁은 며칠에서 한 달 규모라 7일·30일 창이 맞다"이다.

이 실측까지 묶어 41열 전체를 "실제 AML 신호인가, 생성기 습관인가"의 관점에서 판정하는 문서를 만들었다(`피처검토_판정_페이블_20260916.md`, 에이전트 작성·본인 검토). 요지는 다음과 같다.

- 정답지를 직접 베끼는 열은 없다. 다만 누적형 3열(계좌쌍 반복 수·송신 누적 차수·수신 누적 차수)은 값이 관측 경과와 함께 커지는 시계 역할을 하고, 그 이동이 분포 꼬리가 아니라 본체에서 일어나므로 상한이 아니라 창 방식으로 다시 만들어야 한다.
- "처음 만나는 계좌쌍" 신호는 방향은 맞지만 크기가 생성기 때문에 부풀려졌다. 8종 패턴 모두에서 첫 만남 비율이 83~92%로 균일하고, 정상 거래의 중앙값 계좌쌍은 이미 100~150회 거래한 사이다. 이진 1열로 줄여 쓰는 것을 제안했다.
- 통화 15열은 8종 패턴 행의 98.6%가 8개 통화에 몰려 있고 나머지 7개 통화(행의 19%)에는 1.4%뿐이다. 모델이 배우는 것은 "이 통화면 패턴 없음"이라는 생성기 지리다. 비트코인 우도비 0.39는 실세계 방향과 반대다. 화두 8 재검토 안건으로 올린다.
- 반올림 금액 플래그는 둥근 금액이면 정상 쪽(우도비 0.086)으로 학습되어 실무 레드플래그와 방향이 반대다. 구조화 상수 9,800·49,000은 통화 무관이라 엔·루피에서는 수십 달러다.
- 임의 상수 6종(1시간·24시간·구조화 2종·반올림·휴면 3.0/2.0) 각각에 데이터로 정하는 방법과 대체안을 붙였다. 빠진 신호로 7일·30일 창 건수와 신규 상대 수, 통과 자금 비율, 역방향 쌍, 은행 관계(교차은행·은행쌍)를 정리했고, 전처리 재실행이 필요한 것은 한 번에 몰아서 하도록 우선순위를 매겼다.

판정 문서의 첫 순위 항목(P0-1)만 실행했다. 41열에서 빼도 차이가 없거나 오른 12열(송신 속도 4·수신 속도 3·구조화 3·휴면 급증·송신 경과시간)을 한꺼번에 뺀 29열 팔을 3시드로 재학습·채점하는 실측이다. 코드의 41열은 그대로이고, 제거 여부는 결과를 보고 팀에서 정한다.

- 산출물 경로 : `/workspace/feat_final_20260915/` 안의 `m6_verdict_table.csv`(판정표 재조립)·`m6_verdict.py`(분기 버그 수정)·`피처정제_최종판정표_검토_손은총_20260915.ipynb`(그림 5장), `hist_hypothesis.csv`·`run_hist_hypothesis.sh`·`run_hist_hypothesis2.sh`(누적 차수·경과시간 단독 제거 5팔), `split43_hypothesis.csv`·`run_43_single_drops.sh`(43열 기준 2열 분리 3팔), `m4b_full_gain.py`·`m4b_full_gain_summary.csv`(재구성 동원 열 수), `fig_refine_journey.py`·`.csv`·`.png`(81→41 여정), `m9_pattern_speed.py`·`m9_attempt_speed.csv`·`m9_window_fit.csv`(세탁 속도 실측), `페이블_피처검토_프롬프트_20260916.txt`·`피처검토_판정_페이블_20260916.md`(41열 판정 문서), `run_p0_1_drop12.sh`·`p0_1_drop12.csv`·`_p0_1.log`(12열 제거 3시드 실측). 세션 인계 문서 `/workspace/세션상태_손은총_20260916.md`.

- 진행 중인 일 + 현재 단계 :

12열 제거 팔 3시드 실측이 07:55 UTC에 시작해 시드 42와 7이 끝났다. 정밀도는 시드 42가 0.8548(대조군 0.8460, +0.87%p), 시드 7이 0.8545(대조군 0.8457, +0.88%p)다. 재현율 0.70에 필요한 경보 수도 22,697에서 22,465로 줄었다. 두 시드 모두 흔들림 폭 0.14%p를 넘어 오른 방향이고, 묶음별 변화(+0.05, +0.21, +0.11)와 송신 경과시간 단독(+0.59)을 합친 예상치와 방향이 같다. 시드 123은 08:26 UTC에 시작해 약 16분 뒤 끝난다. 세 시드가 모이면 대조군과 나란히 표로 정리한다.

오늘 새벽 에이전트가 단독 제거 실측 결과를 "제거 확정"으로 기록하고 그 위에서 다른 열의 필요성 수치까지 미리 갱신한 것을 발견해 전부 "실측만 끝남, 결정 아님"으로 고쳤다. 현재 41열 코드는 09-14 결정 그대로이고 실제로 뺀 열은 하나도 없다. 앞으로 실험 결과에서 나온 추천은 승인 전까지 "제안·미결"로만 적는다.

## 문제 상황

- 막힌 내용 :

피처를 만들 때 넣은 상수에 근거 기록이 없었다. 1시간·24시간 창, 구조화 9,800·49,000, 반올림 1만 센트, 휴면 3.0·2.0이 전부 코드에 숫자로만 있다. 회의에서 근거를 물었을 때 답을 못 한 이유가 이것이다. 오늘 실측으로 창 크기의 답은 마련했지만, 나머지 상수도 같은 방식으로 "근거 있음 / 임시값 / 폐기 제안" 중 하나를 붙여야 한다. 그리고 판정 문서에서 정당하지 않다고 본 열(통화 15열, 반올림 플래그)과 창 방식으로 다시 만들어야 하는 누적형 3열은 성능이 아니라 정당성 문제라, 성적표만으로는 팀을 설득할 수 없고 근거를 직접 보여야 한다. 누적형 열을 창 방식으로 바꾸는 것과 7일·30일 창, 은행 관계 열을 넣는 것은 전처리 재실행(180M행)이 필요해서 후보를 다 모아 한 번에 돌려야 한다.

- 필요한 협업 또는 결정권자 :

팀 안건으로 올릴 것. ① 12열 제거 제안(3시드 실측 결과 첨부) 승인 여부. ② 통화 15열을 화두 8 "통화 ID 보존" 결정대로 남길지, 패턴이 8개 통화에만 있다는 실측을 근거로 뺄지. ③ 처음 만나는 계좌쌍 신호를 이진 1열로 줄일지. ④ 누적형 3열을 7일·30일 창 방식으로 다시 만들지(전처리 재실행 1회). ⑤ 자기송금 열(화두 11 재논의). ⑥ 은행 두 열을 피처로 쓸지 — 먼저 패턴 행과 정상 행의 교차은행 비율을 재서 99%를 넘으면 ACH처럼 생성기 규칙으로 본다. 그 밖에 다중분류 모델 계열(트리 vs GNN)이 정해져야 "필요성" 결론을 그 모델 기준으로 다시 잴 수 있다.

## 내일 계획

- 가장 먼저 할 일 : 12열 제거 3시드 결과를 대조군과 나란히 표로 정리해 팀 안건 문서에 넣는다. 그다음 원본만으로 잴 수 있는 사전 측정 세 가지(패턴·정상의 교차은행 비율, 구조화 문턱 근방 밀도 불연속, 시각 우도비)를 해서 은행 열과 상수 대체안의 근거를 채운다. 회의 질문에 대한 답(1시간·24시간 창의 실측 근거와 7일·30일 제안)을 한 장으로 정리해 공유한다.
- 완료 기준 : 12열 제거 팔 3시드 정밀도가 대조군 3시드와 한 표에 있고, 임의 상수 6종 각각에 "근거 있음 / 임시값 / 폐기 제안" 표시가 붙고, 팀 안건 6개가 실측 수치와 함께 문서 한 장에 들어가 있으면 마무리로 본다. 실제 열 제거와 전처리 재실행은 팀 승인 뒤에 한다.
