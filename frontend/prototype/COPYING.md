# Prototype history import

Versions 1–20 were copied from the read-only product design Prototype archive into this repository. The originals were not moved, edited, or deleted.

Included:

- Review SVG/HTML and visual QA images
- Source code, tests, package manifests, and build configuration
- README and non-sensitive design-review inventories

Excluded:

- `.DS_Store`, `node_modules/`, `dist/`, `coverage/`, and `*.tsbuildinfo`
- Meeting-source documents and personal handoff prompts
- `.obsidian`, PARA structure, private skill directories, and the wider `Frontend/Media` directory

Version 21 was created from version 20 source. Its only non-code graph resource is the copied `src/data/graph-blocks.json` fixture.
