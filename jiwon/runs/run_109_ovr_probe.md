# run_109~110 — OvR 프로브 (전용 이진 vs 파생 p_k)

- 상태: 완료 — 동률, **OvR 전면 구축 기각**
- 기준선: run_101 의 클래스별 파생 점수 (ovr_pr_auc: FAN-IN 0.304, BIPARTITE 0.143)
- 단일 변인: **모델 구조** — 해당 클래스 전용 이진 LightGBM (train 데이터·표본·
  011b 파라미터(λ=100 등)·분할·평가 전부 run_101 과 동일, objective 만 binary)
- 피처: features_v1 61개 (Large 이식판)
- 배경: 패턴별 이진 모델 앙상블(OvR) 구조 제안(2026-08-27 논의)의 저비용 프로브.
  run_015(전용 이진 vs p_NONPAT) 구도를 패턴 클래스에 적용. 전면 OvR 구축(10배
  비용)은 이 프로브가 이길 때만 논의. 트랙 소유권 정리도 그때.

| run | 클래스 | 선정 이유 |
|---|---|---|
| 109 | FAN-IN (label 2) | 중위 신호 클래스 — 대표성 |
| 110 | BIPARTITE (label 7) | 최약 신호 + BP↔STACK 혼동 당사자 — 독립 점수의 이득이 있다면 여기서 |

- 판정 지표: 해당 클래스 이진 PR-AUC (run_101 ovr_pr_auc 와 동일 정의) +
  클래스 닻 P@R{0.5,0.7,0.9}.
- 가설: softmax 억제 제거의 이득 < 배타 라벨의 경쟁 정보 손실 — 파생 p_k 와
  동률 또는 열세 예상 (run_014 의 분해 불변 결과와 같은 방향). 이기면 OvR
  전면 구축을 팀 안건으로 올린다.

## 결과 (val 전량)

| 클래스 | 전용 이진 PR-AUC | 파생 p_k (run_101) | 시드 노이즈(101↔102) |
|---|---|---|---|
| FAN-IN (109) | 0.3104 | 0.304 | ±0.006 |
| BIPARTITE (110) | 0.1449 | 0.143 | ±0.006 |

- 두 클래스 모두 차이가 시드 노이즈 폭 이내 — **완전 동률**. 최약 신호(BIPARTITE)
  에서도 독립 이진화의 이득 없음. 가설(경쟁 정보 손실 ≈ softmax 억제 제거) 확증,
  run_014(분해 불변)와 일관.
- 부수 실측: 이진 run 은 학습+predict 9분(다중클래스 ~2h 의 1/13) — 클래스당
  트리 1/10 효과. 전면 OvR(9모델)을 만들어도 비용은 오히려 다중클래스와 비슷.
  그러나 성능 이득이 0 이므로 구축 근거 자체가 없음.
- 판정: **OvR 전면 구축 기각.** 패턴별 점수 표시·설명 요구는 현행 10클래스의
  p_k 로 충족(동일 품질이 실측 확인됨). 트랙 소유권 논의도 불필요해짐.
